import React from "react";
import Tilt from "react-parallax-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        I’m a software developer with a unique academic foundation that blends technology and human centered problem solving, holding a B.S. in Computer Science from Franklin University and a prior B.S. in Health Science & Rehabilitation from The Ohio State University. Over the course of my career, I have developed a versatile technical skill set spanning full stack web development, mobile application design, and database management, working extensively with Java, Python, JavaScript, React, Node.js, Spring Boot, MongoDB, and MySQL. My professional experience includes winning JPMorgan Chase’s Code For Good Hackathon where my team built a high impact community engagement platform, as well as delivering a production ready e commerce platform with secure authentication and cloud integration during my software development co op. I am driven by a passion for creating innovative, scalable solutions that improve quality of life, and I aim to leverage technology to solve meaningful challenges while contributing to the growth of the tech community.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");